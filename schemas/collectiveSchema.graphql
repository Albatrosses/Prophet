schema {
  query: Query
}

type Query {
  getUserInfo(id: Int): UserInfo
}

type UserInfo {
  id: Int!,
  name: String,
  isViewerFriend: Boolean,
  getProfilePicture(size: Int): profilePicture
}

type profilePicture {
  uri: String,
  width: Int,
  height: Int
}